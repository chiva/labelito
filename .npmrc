# Disable lifecycle scripts for every install. The icon collections are pure SVG-asset packages with
# no build step, so blocking pre/post/install scripts removes a supply-chain code-execution vector.
ignore-scripts=true
